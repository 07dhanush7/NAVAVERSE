const RouteLoader = () => (
  <div className="route-loader" role="status" aria-live="polite">
    <span className="route-loader-spinner" aria-hidden="true" />
    <span>Loading NAVAVERSE...</span>
  </div>
);

export default RouteLoader;
